import {ArrowLeft,ArrowRight,Eye,EyeOff,ShieldCheck} from "lucide-react"; import {FormEvent,useState} from "react"; import {Link} from "react-router-dom"; import Logo from "../../components/common/Logo"; import Input from "../../components/common/Input";
export default function SignIn(){const[show,setShow]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault()}return <main className="auth-page"><Link className="auth-back" to="/"><ArrowLeft size={15}/> Milan</Link><div className="auth-logo"><Logo/></div><div className="auth-intro"><span className="section-kicker">Welcome back</span><h1>Good to see you.</h1><p>Sign in and continue your journey towards something meaningful.</p></div><form className="auth-form" onSubmit={submit}><Input id="email" label="Email or phone" placeholder="you@example.com" autoComplete="username"/><div className="password-wrap"><Input id="password" label="Password" type={show?"text":"password"} placeholder="Your password" autoComplete="current-password"/><button className="show-password" type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div><div className="auth-meta"><label><input type="checkbox"/> Remember me</label><Link to="/sign-in">Forgot password?</Link></div><button className="button button-primary button-full" type="submit">Sign in <ArrowRight size={16}/></button></form><div className="auth-divider"><span>or</span></div><Link className="auth-create" to="/sign-up">New to Milan? <strong>Create an account</strong></Link><div className="auth-trust"><ShieldCheck size={14}/> Your personal details are protected.</div></main>}